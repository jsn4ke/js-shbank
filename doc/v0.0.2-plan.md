# BSH v0.0.2 详细开发计划

## 目标

基于 v0.0.1 的成功完成（测试覆盖率 95%，成功获取 260 个理财产品），v0.0.2 将进行重大升级，使 BSH 成为可被第三方快速引用的 Python 库，并添加本地 Web 查看功能和产品详情页 URL 定位。

### 核心目标

1. **第三方库化** - BSH 能够被其他项目通过 `pip install` 安装和 `import js_shbank` 引用
2. **数据结构暴露** - 提供清晰的 ProductModel 和其他数据模型接口
3. **本地 Web 查看** - 提供 Streamlit Web 界面查看产品数据
4. **产品 URL 定位** - 为每个产品添加详情页 URL 字段，可链接到上海银行官网

---

## 技术方案

| 需求 | 技术选型 | 理由 |
|------|----------|------|
| Web 框架 | Streamlit | 轻量级、数据展示强、Python 原生、易集成 |
| 包管理 | pyproject.toml + setuptools | 标准化、与现有配置兼容 |
| URL 生成 | 基于上海银行 URL 模式推断 | 稳定、可预测 |

---

## 任务分解 (按开发顺序)

### 阶段一：库化基础设施 (Tasks 1-3)

#### Task 1: 完善模块导出
**分支**: `feat/library-exports`

**目标**: 让所有模块可以被外部项目正确导入

**需要修改的文件**:
- `src/__init__.py`
- `src/models/__init__.py`
- `src/config/__init__.py`
- `src/scraper/__init__.py`
- `src/repository/__init__.py`
- `src/calculator/__init__.py`

**实现内容**:

##### 1.1 `src/__init__.py`
```python
"""BSH - 上海银行理财数据爬取库"""

__version__ = "0.0.2"

from js_shbank.models import ProductModel
from js_shbank.config import Settings
from js_shbank.scraper import ApiClient, Parser
from js_shbank.repository import CsvRepository, RepositoryFactory
from js_shbank.calculator import YieldCalculator

__all__ = [
    "__version__",
    "ProductModel",
    "Settings",
    "ApiClient",
    "Parser",
    "CsvRepository",
    "RepositoryFactory",
    "YieldCalculator",
]
```

##### 1.2 `src/models/__init__.py`
```python
"""数据模型模块"""

from js_shbank.models.product import ProductModel

__all__ = ["ProductModel"]
```

##### 1.3 `src/config/__init__.py`
```python
"""配置管理模块"""

from js_shbank.config.settings import Settings

__all__ = ["Settings"]
```

##### 1.4 `src/scraper/__init__.py`
```python
"""数据爬取模块"""

from js_shbank.scraper.api_client import ApiClient
from js_shbank.scraper.parser import Parser

__all__ = ["ApiClient", "Parser"]
```

##### 1.5 `src/repository/__init__.py`
```python
"""数据存储模块"""

from js_shbank.repository.base import BaseRepository
from js_shbank.repository.csv_repository import CsvRepository
from js_shbank.repository.factory import RepositoryFactory

__all__ = ["BaseRepository", "CsvRepository", "RepositoryFactory"]
```

##### 1.6 `src/calculator/__init__.py`
```python
"""收益率计算模块"""

from js_shbank.calculator.yield_calculator import YieldCalculator

__all__ = ["YieldCalculator"]
```

**测试点**:
- [ ] `import js_shbank` 成功
- [ ] `from js_shbank import ProductModel` 成功
- [ ] `from js_shbank.models import ProductModel` 成功
- [ ] `bsh.__version__` 返回 "0.0.2"
- [ ] 所有 `__all__` 导出的成员都可以访问

---

#### Task 2: 添加命令行入口点
**分支**: `feat/cli-entrypoint`

**目标**: 提供 `bsh-fetch` 和 `bsh-web` 命令用于获取数据和启动 Web 界面

**需要修改的文件**:
- `pyproject.toml` - 添加 `[project.scripts]` 配置
- `src/cli.py` - 新建 CLI 模块

**实现内容**:

##### 2.1 `pyproject.toml` 添加入口点
```toml
[project.scripts]
bsh-fetch = "bsh.cli:fetch_command"
bsh-web = "bsh.cli:web_command"
```

##### 2.2 `src/cli.py` 新建文件
```python
"""命令行入口模块"""

import sys
import argparse
from js_shbank.scraper import ApiClient, Parser
from js_shbank.repository import RepositoryFactory
from js_shbank.config import Settings


def fetch_command() -> None:
    """获取产品数据并保存到 CSV"""
    parser = argparse.ArgumentParser(description="获取上海银行理财产品数据")
    parser.add_argument("--output", "-o", help="输出文件路径", default="data/products.csv")
    parser.add_argument("--page-size", help="每页数量", type=int, default=100)
    args = parser.parse_args()

    settings = Settings()
    settings.page_size = args.page_size

    print(f"正在获取产品数据 (每页 {args.page_size} 条)...")

    client = ApiClient(settings)
    parser_obj = Parser()
    repository = RepositoryFactory.create("csv", filepath=args.output)

    total_fetched = 0
    page = 1

    try:
        for page_data in client.fetch_all_products():
            products = parser_obj.parse_products(page_data)
            if products:
                repository.save_batch(products)
                total_fetched += len(products)
                print(f"已获取 {total_fetched} 条产品数据...")

        print(f"\n完成！共获取 {total_fetched} 条产品数据")
        print(f"数据已保存到: {args.output}")

    except Exception as e:
        print(f"错误: {e}", file=sys.stderr)
        sys.exit(1)


def web_command() -> None:
    """启动 Web 查看界面"""
    import subprocess
    import sys

    print("启动 BSH Web 查看界面...")
    subprocess.run([sys.executable, "-m", "streamlit", "run", "bsh.web.app"])


if __name__ == "__main__":
    fetch_command()
```

**测试点**:
- [ ] `bsh-fetch --help` 显示帮助信息
- [ ] `bsh-fetch` 成功获取数据
- [ ] `bsh-fetch -o custom.csv` 指定输出路径
- [ ] `bsh-fetch --page-size 50` 指定分页大小

---

#### Task 3: 版本管理配置
**分支**: `feat/version-management`

**目标**: 统一版本号管理

**需要修改的文件**:
- `pyproject.toml` - 更新版本为 0.0.2
- `src/__init__.py` - 添加 `__version__ = "0.0.2"`

**实现内容**:

##### 3.1 `pyproject.toml`
```toml
version = "0.0.2"
```

##### 3.2 `src/__init__.py`
```python
__version__ = "0.0.2"
```

**测试点**:
- [ ] `import js_shbank; print(bsh.__version__)` 输出 "0.0.2"
- [ ] `pip show bsh` 显示版本 0.0.2

---

### 阶段二：产品 URL 定位 (Tasks 4-5)

#### Task 4: 产品详情 URL 研究
**分支**: `feat/product-url-research`

**目标**: 确定产品详情页 URL 生成规则

**研究内容**:
- 分析上海银行产品页面 URL 模式
- 确定需要的参数（产品代码、产品类别等）
- 验证 URL 有效性

**预期 URL 模式**:
```
https://www.bosc.cn/zh/gryw/tzlc/lc/zxcpxx/?prdId={prd_id}&prdCategory={prd_category}
```

**备选方案**:
```
方案 A: https://www.bosc.cn/zh/gryw/tzlc/lc/zxcpxx/?prdId={prd_id}
方案 B: https://www.bosc.cn/zh/products/{prd_category}/{prd_code}
```

**测试点**:
- [ ] 确定正确的 URL 模式
- [ ] 验证 URL 可以访问
- [ ] 确认需要哪些参数

---

#### Task 5: 扩展 ProductModel
**分支**: `feat/product-url-fields`

**目标**: 为 ProductModel 添加 URL 相关字段

**需要修改的文件**:
- `src/models/product.py` - 添加新字段
- `src/scraper/parser.py` - 添加 URL 生成方法
- `src/repository/csv_repository.py` - 更新 CSV 表头
- `tests/test_product.py` - 添加新字段测试

**实现内容**:

##### 5.1 `src/models/product.py` 新增字段
```python
class ProductModel(BaseModel):
    # ... 现有字段 ...

    # 新增字段
    prd_id: str | None = None  # 产品 ID（用于生成详情页 URL）
    prd_category: str | None = None  # 产品类别（用于生成详情页 URL）
    detail_page_url: str | None = Field(default=None, description="产品详情页 URL")
```

##### 5.2 `src/scraper/parser.py` 添加 URL 生成方法
```python
@staticmethod
def build_detail_url(prd_id: str | None, prd_category: str | None = None) -> str | None:
    """构建产品详情页 URL"""
    if not prd_id:
        return None

    base_url = "https://www.bosc.cn/zh/gryw/tzlc/lc/zxcpxx/"
    params = f"?prdId={prd_id}"
    if prd_category:
        params += f"&prdCategory={prd_category}"
    return base_url + params
```

##### 5.3 `src/repository/csv_repository.py` 更新表头
```python
CSV_HEADER = [
    "prd_code",
    "prd_name",
    "prd_id",
    "prd_category",
    "detail_page_url",
    # ... 其他字段 ...
]
```

**测试点**:
- [ ] ProductModel 可以创建包含新字段的实例
- [ ] URL 生成方法正确生成 URL
- [ ] CSV 正确保存 URL 字段
- [ ] CSV 正确读取 URL 字段

---

### 阶段三：Web 查看功能 (Tasks 6-8)

#### Task 6: Streamlit 基础设施
**分支**: `feat/web-infrastructure`

**目标**: 创建 Web 模块基础结构

**需要新建的文件**:
- `src/web/__init__.py`
- `src/web/app.py`
- `tests/test_web.py`

**需要修改的文件**:
- `pyproject.toml` - 添加 streamlit 依赖

**实现内容**:

##### 6.1 `pyproject.toml` 添加依赖
```toml
dependencies = [
    # ... 现有依赖 ...
    "streamlit>=1.29.0",
]
```

##### 6.2 `src/web/__init__.py`
```python
"""Web 查看模块"""

from js_shbank.web.app import main

__all__ = ["main"]
```

##### 6.3 `src/web/app.py` 主应用
```python
"""Streamlit Web 应用"""

import streamlit as st
import pandas as pd
from js_shbank.repository import RepositoryFactory
from js_shbank.models import ProductModel


@st.cache_data
def load_products(filepath: str = "data/products.csv") -> list[ProductModel]:
    """加载产品数据（带缓存）"""
    repository = RepositoryFactory.create("csv", filepath=filepath)
    return repository.find_all()


def main() -> None:
    """主函数"""
    st.set_page_config(
        page_title="BSH - 上海银行理财产品查询",
        page_icon="🏦",
        layout="wide"
    )

    st.title("🏦 上海银行理财产品查询")
    st.markdown("---")

    # 加载数据
    products = load_products()

    if not products:
        st.warning("暂无产品数据，请先运行 `bsh-fetch` 获取数据")
        return

    # 数据转换为 DataFrame
    df = pd.DataFrame([p.model_dump() for p in products])

    # 侧边栏筛选器
    st.sidebar.header("筛选条件")

    # 按产品代码筛选
    code_filter = st.sidebar.text_input("产品代码（模糊搜索）")

    # 按产品名称筛选
    name_filter = st.sidebar.text_input("产品名称（模糊搜索）")

    # 按风险等级筛选
    risk_levels = ["全部"] + sorted(df["risk_level"].dropna().unique().tolist())
    risk_filter = st.sidebar.selectbox("风险等级", risk_levels)

    # 按币种筛选
    currencies = ["全部"] + sorted(df["curr_type"].dropna().unique().tolist())
    currency_filter = st.sidebar.selectbox("币种", currencies)

    # 应用筛选
    filtered_df = df.copy()

    if code_filter:
        filtered_df = filtered_df[filtered_df["prd_code"].str.contains(code_filter, case=False, na=False)]

    if name_filter:
        filtered_df = filtered_df[filtered_df["prd_name"].str.contains(name_filter, case=False, na=False)]

    if risk_filter != "全部":
        filtered_df = filtered_df[filtered_df["risk_level"] == risk_filter]

    if currency_filter != "全部":
        filtered_df = filtered_df[filtered_df["curr_type"] == currency_filter]

    # 显示结果
    st.subheader(f"查询结果: {len(filtered_df)} 条")

    if len(filtered_df) > 0:
        st.dataframe(
            filtered_df,
            column_config={
                "prd_code": st.column_config.TextColumn("产品代码", width=150),
                "prd_name": st.column_config.TextColumn("产品名称", width=300),
                "rate": st.column_config.NumberColumn("年化收益率(%)", format="%.2f"),
                "risk_level": st.column_config.TextColumn("风险等级", width=100),
                "curr_type": st.column_config.TextColumn("币种", width=80),
                "pfirst_amt": st.column_config.NumberColumn("起售金额", format="%.0f"),
                "sold_out": st.column_config.CheckboxColumn("是否售罄"),
            },
            use_container_width=True
            hide_index=True,
        )
    else:
        st.info("没有匹配的产品")


if __name__ == "__main__":
    main()
```

**测试点**:
- [ ] Streamlit 应用可以启动
- [ ] 数据正确加载
- [ ] 基本筛选器工作正常

---

#### Task 7: 增强 Web 功能
**分支**: `feat/web-features`

**目标**: 添加更多 Web 查看功能

**需要修改的文件**:
- `src/web/app.py` - 添加更多功能

**新增功能**:

##### 7.1 数据统计面板
```python
# 风险等级分布
st.subheader("风险等级分布")
risk_counts = df["risk_level"].value_counts()
st.bar_chart(risk_counts)

# 币种统计
st.subheader("币种统计")
currency_counts = df["curr_type"].value_counts()
st.bar_chart(currency_counts)
```

##### 7.2 产品详情视图
```python
# 选择产品查看详情
if len(filtered_df) > 0:
    selected_product = st.selectbox(
        "选择产品查看详情",
        options=filtered_df["prd_code"].tolist(),
        format_func=lambda x: filtered_df[filtered_df["prd_code"] == x]["prd_name"].iloc[0]
    )

    if selected_product:
        product_data = filtered_df[filtered_df["prd_code"] == selected_product].iloc[0]
        st.json(product_data.to_dict())
```

##### 7.3 搜索功能
- 添加全局搜索框
- 支持多字段搜索

**测试点**:
- [ ] 统计图表正确显示
- [ ] 产品详情视图正常
- [ ] 搜索功能正常

---

#### Task 8: Web 模块测试
**分支**: `test/web-tests`

**目标**: 完善 Web 模块测试

**需要修改的文件**:
- `tests/test_web.py` - 添加完整测试

**测试内容**:

##### 8.1 测试用例
```python
import pytest
from unittest.mock import patch, MagicMock
from js_shbank.web.app import load_products


def test_load_products():
    """测试加载产品数据"""
    with patch("bsh.web.app.RepositoryFactory") as mock_factory:
        mock_repo = MagicMock()
        mock_repo.find_all.return_value = []
        mock_factory.create.return_value = mock_repo

        products = load_products("test.csv")
        mock_factory.create.assert_called_once_with("csv", filepath="test.csv")
        mock_repo.find_all.assert_called_once()


def test_filter_by_code():
    """测试按产品代码筛选"""
    # 筛选逻辑测试
    pass


def test_filter_by_risk_level():
    """测试按风险等级筛选"""
    # 筛选逻辑测试
    pass


def test_filter_by_currency():
    """测试按币种筛选"""
    # 筛选逻辑测试
    pass


def test_combined_filters():
    """测试组合筛选"""
    # 组合筛选测试
    pass


def test_empty_data():
    """测试空数据处理"""
    # 空数据测试
    pass
```

**测试点**:
- [ ] 加载产品数据测试
- [ ] 筛选逻辑测试（按代码、名称、风险等级、币种）
- [ ] 组合筛选测试
- [ ] 边界情况测试

---

### 阶段四：文档与发布 (Tasks 9-10)

#### Task 9: 更新文档
**分支**: `docs/update-v0.0.2-docs`

**目标**: 更新项目文档

**需要修改的文件**:
- `README.md` - 更新使用说明
- `memory/project-progress.md` - 更新进度
- `doc/v0.0.2-plan.md` - 新建本文档（本文档）

**更新内容**:

##### 9.1 `README.md` 新增章节
```markdown
## 作为库使用

BSH 可以作为一个 Python 库被其他项目引用。

### 安装

```bash
pip install bsh
```

### 基本使用

```python
from bsh import ApiClient, Parser, CsvRepository, Settings

# 创建配置
settings = Settings()

# 获取产品数据
client = ApiClient(settings)
parser = Parser()

products = []
for page_data in client.fetch_all_products():
    products.extend(parser.parse_products(page_data))

# 保存到 CSV
repository = CsvRepository(filepath="products.csv")
repository.save_batch(products)
```

### 使用数据模型

```python
from bsh import ProductModel

# 创建产品
product = ProductModel(
    prd_code="B12345",
    prd_name="示例产品",
    rate=3.5,
    risk_level="R3中等风险"
)

# 访问字段
print(product.prd_name)
print(product.detail_page_url)
```

## Web 查看界面

BSH 提供基于 Streamlit 的 Web 查看界面。

### 启动 Web 界面

```bash
# 方法 1: 使用命令行工具
bsh-web

# 方法 2: 直接运行
streamlit run bsh/web/app.py
```

### 功能特性

- 📊 产品列表查看
- 🔍 多条件筛选（代码、名称、风险等级、币种）
- 📈 数据统计面板
- 🔗 产品详情页链接
```

##### 9.2 `memory/project-progress.md` 更新
```markdown
## 版本: v0.0.2

> 目标: 库化、Web 查看、产品 URL 定位

### 任务清单 (按开发顺序)

| ID | 任务名称 | 分支名 | 状态 | 说明 |
|----|----------|--------|------|------|
| 1 | 完善模块导出 | feat/library-exports | ⏳ 待开始 | 添加 __all__ 导出 |
| 2 | 添加命令行入口点 | feat/cli-entrypoint | ⏳ 待开始 | CLI 命令 |
| 3 | 版本管理配置 | feat/version-management | ⏳ 待开始 | 更新版本号 |
| 4 | 产品详情 URL 研究 | feat/product-url-research | ⏳ 待开始 | 确定 URL 模式 |
| 5 | 扩展 ProductModel | feat/product-url-fields | ⏳ 待开始 | 添加 URL 字段 |
| 6 | Streamlit 基础设施 | feat/web-infrastructure | ⏳ 待开始 | 创建 Web 模块 |
| 7 | 增强 Web 功能 | feat/web-features | ⏳ 待开始 | 统计、详情视图 |
| 8 | Web 模块测试 | test/web-tests | ⏳ 待开始 | Web 测试 |
| 9 | 更新文档 | docs/update-v0.0.2-docs | ⏳ 待开始 | README 更新 |
| 10 | 打包与发布准备 | chore/package-for-release | ⏳ 待开始 | MANIFEST.in, LICENSE |
```

**测试点**:
- [ ] README 文档完整
- [ ] 进度追踪文档更新
- [ ] 代码注释完善

---

#### Task 10: 打包与发布准备
**分支**: `chore/package-for-release`

**目标**: 准备发布 v0.0.2 版本

**需要修改的文件**:
- `MANIFEST.in` - 新建，指定打包文件
- `LICENSE` - 确认许可证文件存在

**实现内容**:

##### 10.1 `MANIFEST.in`
```ini
include README.md
include LICENSE
include pyproject.toml
recursive-include src *.py
recursive-include data *.csv
```

##### 10.2 验证 LICENSE
```bash
cat LICENSE
# 确认 MIT 许可证存在
```

**测试点**:
- [ ] `python -m build` 成功构建包
- [ ] `twine check dist/*` 验证包元数据
- [ ] 本地安装测试成功

---

## 关键文件清单

实现此计划需要修改的关键文件：

| 文件 | 修改类型 | 相关任务 |
|------|----------|----------|
| `pyproject.toml` | 修改 | T2, T3, T6, T10 |
| `src/__init__.py` | 修改 | T1, T3 |
| `src/cli.py` | 新建 | T2 |
| `src/models/__init__.py` | 修改 | T1 |
| `src/models/product.py` | 修改 | T5 |
| `src/config/__init__.py` | 修改 | T1 |
| `src/scraper/__init__.py` | 修改 | T1 |
| `src/scraper/parser.py` | 修改 | T5 |
| `src/repository/__init__.py` | 修改 | T1 |
| `src/repository/csv_repository.py` | 修改 | T5 |
| `src/calculator/__init__.py` | 修改 | T1 |
| `src/web/__init__.py` | 新建 | T6 |
| `src/web/app.py` | 新建 | T6, T7 |
| `tests/test_web.py` | 新建 | T8 |
| `README.md` | 修改 | T9 |
| `MANIFEST.in` | 新建 | T10 |

---

## 开发顺序

```
阶段一：库化基础设施（可并行）
├── Task 1: 完善模块导出
├── Task 2: 添加命令行入口点 (依赖 Task 1)
└── Task 3: 版本管理配置

阶段二：产品 URL 定位（顺序）
├── Task 4: 产品详情 URL 研究
└── Task 5: 扩展 ProductModel (依赖 Task 4)

阶段三：Web 查看功能（顺序）
├── Task 6: Streamlit 基础设施
├── Task 7: 增强 Web 功能 (依赖 Task 6)
└── Task 8: Web 模块测试 (依赖 Task 7)

阶段四：文档与发布（顺序）
├── Task 9: 更新文档 (依赖前面所有阶段)
└── Task 10: 打包与发布准备 (依赖 Task 9)
```

---

## 验收标准

### 功能验收

#### 1. 第三方库化
- [ ] 可以 `import js_shbank` 导入库
- [ ] 可以导入所有公共类和函数
- [ ] 版本号正确显示
- [ ] 命令行工具可以执行

#### 2. 数据结构暴露
- [ ] ProductModel 可以直接使用
- [ ] APIResponse 可以直接使用
- [ ] 所有字段文档完整

#### 3. 本地 Web 查看
- [ ] Streamlit 应用可以启动
- [ ] 数据正确加载和显示
- [ ] 筛选功能正常
- [ ] 统计图表正确
- [ ] 产品详情完整

#### 4. 产品 URL 定位
- [ ] ProductModel 包含 URL 字段
- [ ] URL 可以正确生成
- [ ] URL 链接可点击
- [ ] CSV 正确保存 URL

### 质量验收
- [ ] 所有测试通过
- [ ] 测试覆盖率 ≥ 80%
- [ ] 代码通过 ruff 检查
- [ ] 代码通过 mypy 类型检查
- [ ] 文档完整准确

---

## 潜在风险与应对

| 风险 | 影响 | 应对措施 |
|------|------|----------|
| 产品详情 URL 规律不明确 | 高 | Task 4 重点研究，准备多种方案 |
| Streamlit 性能问题 | 中 | 使用缓存机制，限制显示数量 |
| CSV 兼容性问题 | 中 | 做好旧数据迁移测试 |
| 第三方依赖冲突 | 低 | 使用可选依赖，明确版本要求 |

---

## 时间估算

| 阶段 | 任务数 | 预计时间 |
|------|--------|----------|
| 阶段一：库化基础设施 | 3 | 2-3 小时 |
| 阶段二：产品 URL 定位 | 2 | 2-3 小时 |
| 阶段三：Web 查看功能 | 3 | 4-5 小时 |
| 阶段四：文档与发布 | 2 | 1-2 小时 |
| **总计** | **10** | **9-13 小时** |

---

## 开发流程规范 (CRITICAL)

```
每个任务必须严格遵循以下流程:

1. [Planning]  制定小功能点实现计划
2. [Branch]    创建新分支 (从 main 拉出)
3. [TDD]       写测试 → 实现代码 → 验证通过
4. [Commit]    提交到本地分支
5. [Push]      推送到远程分支
6. [Test]      严格测试（单元/集成）
7. [Merge]     合并到 main
8. [Push]      推送 main 到远程
```

### 关键约束

| 阶段 | 要求 |
|------|------|
| Python 操作 | **所有 Python 相关操作必须在 venv 虚拟环境下执行** |
| 开发前 | 制定计划，明确小功能点 |
| 开发中 | TDD：先写测试，再写代码 |
| 提交前 | 测试必须通过，覆盖率 ≥80% |
| 合并前 | 所有测试通过 |
| 合并后 | 立即推送 main |

### 虚拟环境激活

```bash
# Windows
venv\Scripts\activate

# Linux/Mac
source venv/bin/activate
```

---

## 下一步计划

v0.0.3 功能（待规划）:
- [ ] 定时任务支持
- [ ] 数据变更通知
- [ ] 更多图表类型
- [ ] 数据导出功能增强
